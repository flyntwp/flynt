{% extends 'twig/layouts/default.twig' %}

{% block header %}
  {{ renderComponent('NavigationMain') }}
{% endblock %}

{% block components %}
  {{ renderFlexibleContent(post.meta('pageComponents')) }}
  {{ renderFlexibleContent(post.meta('postComponents')) }}
{% endblock %}

{% block footer %}
  {{ renderComponent('NavigationFooter') }}
  {{ renderComponent('BlockCookieNotice') }}
{% endblock %}
