div(class=$data('baseClasses'))
  h2=$data('title')
  ul
    for post in $data('posts')
      li
        a(href=post.url)=post.title
        p=post.content
        if(post.image)
          a(href=post.url)
            img(src=post.image)
