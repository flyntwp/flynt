<div is="flynt-block-image-text-cta" class="flyntComponent {{ options.theme }}">
  <div class="container centerMaxWidthContainer">
    <div class="box box--{{ imagePosition }}">
      <div class="box-inner box-inner--content">
        {{ contentHtml|e('wp_kses_post') }}
      </div>
      <div class="box-inner box-inner--image">
        <figure class="figure">
          <img class="figure-image lazyload"
            src="{{ image.src|resize(665) }}"
            srcset="{{ placeholderImage(665, 665 / image.aspect) }}"
            data-srcset="
              {{ image.src|resize(920) }} 920w,
              {{ image.src|resize(815) }} 815w,
              {{ image.src|resize(740) }} 740w,
              {{ image.src|resize(665) }} 665w,
              {{ image.src|resize(375) }} 375w"
            data-sizes="auto"
            alt="{{ image.alt|e }}">
          {% if image.caption %}
            <figcaption class="figure-caption">{{ image.caption|e }}</figcaption>
          {% endif %}
        </figure>
      </div>
    </div>
  </div>
</div>
