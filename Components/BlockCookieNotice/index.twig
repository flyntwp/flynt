<div is="flynt-block-cookie-notice" class="flyntComponent {{ layout }} {{ theme ? theme : 'themeDefault' }}">
  <div class="container centerMaxWidthContainer">
    <div class="content">
      {{ contentHtml|e('wp_kses_post') }}
    </div>
    <button type="button" class="button button--primary button--small" aria-label="Close" data-close>{{ closeButtonLabel|e }}</button>
  </div>
</div>
