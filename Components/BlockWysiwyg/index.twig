<flynt-component name="BlockWysiwyg" class="componentSpacing" {{ options.theme ? 'data-theme="' ~ options.theme ~ '"' }}>
  <div class="container">
    <div class="content {{- options.maxWidth != 'full' ? ' centerContentMaxWidth'}}{{ options.maxWidth == 'large' ? 'Wide' }}" {{ textAlign ? 'data-text-align="' ~ textAlign ~ '"' }}>
      {{ contentHtml|e('wp_kses_post') }}
    </div>
  </div>
</flynt-component>
