<flynt-component name="BlockWysiwyg" class="componentSpacing" {{ options.theme ? 'data-theme="' ~ options.theme ~ '"' }}>
  <div class="container centerContainer maxWidth--container">
    <div class="content {{- options.maxWidth != 'full' ? ' centerContainer maxWidth--content'}}{{ options.maxWidth == 'large' ? 'Large' }}" {{ textAlign ? 'data-text-align="' ~ textAlign ~ '"' }}>
      {{ contentHtml|e('wp_kses_post') }}
    </div>
  </div>
</flynt-component>
