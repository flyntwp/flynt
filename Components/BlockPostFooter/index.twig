<div class="flyntComponent" is="flynt-block-post-footer">
  <div class="container centerMaxWidthContainer">
    <div class="content centerContentMaxWidth">
      <hr>
      {% if post.terms('tag')  %}
        <ul class="tags">
          {% for tag in post.terms('tag')  %}
            <li class="tags-item"><a class="tags-itemLink pill" href="{{ tag.link }}">{{ tag }}</a></li>
          {% endfor %}
        </ul>
      {% endif %}
      <div class="author">
        {% if post.author.avatar %}
          <a class="author-avatarLink" href="{{ post.author.link|e('esc_url') }}">
            <img class="author-avatarImage" src="{{ post.author.avatar }}" alt="{{ post.author.name|e }}">
          </a>
        {% endif %}
        <div class="author-info">
          <a class="author-name" href="{{ post.author.link|e('esc_url') }}">{{ post.author.name }}</a>
          {% if post.author.description %}
            <p class="author-description">{{ post.author.description|e }}</p>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</div>
