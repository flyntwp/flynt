<div class="flyntComponent" is="flynt-block-post-footer">
  <div class="container">
    <div class="content">
      {% if post.terms('tag')  %}
        <div class="content-tags">
          <ul class="content-tagList">
            {% for tag in post.terms('tag')  %}
              <li class="content-tagItem"><a class="content-tagLink pill" href="{{ tag.link }}"><span itemprop="keywords">{{ tag }}</span></a></li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}
      <div class="content-author" itemprop="author" itemscope itemtype="http://schema.org/Person">
        {% if post.author.avatar %}
          <a class="content-authorAvatar" href="{{ post.author.link|e('esc_url') }}" itemprop="url">
            <img class="content-authorImage" src="{{ post.author.avatar }}" alt="{{ post.author.name|e }}" itemprop="image">
          </a>
        {% endif %}
        <div class="content-authorInfo">
          <a class="content-authorName" href="{{ post.author.link|e('esc_url') }}">
            <span itemprop="name">{{ post.author.name }}</span>
          </a>
          {% if post.author.description %}
            <p class="content-authorDescription" itemprop="description">{{ post.author.description|e }}</p>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</div>
