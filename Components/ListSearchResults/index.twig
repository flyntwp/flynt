<flynt-component name="ListSearchResults" load:on="visible" class="componentSpacing">
  <div class="container centerMaxWidthContainer">
    <div class="centerContentMaxWidth">
      {% include 'Partials/_header.twig' %}
      {% include 'Partials/_results.twig' %}
      {% if pagination.pages %}
        {% include 'Partials/_pagination.twig' %}
      {% endif %}
    </div>
  </div>
</flynt-component>
