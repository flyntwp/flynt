@import 'config.styl'

[is="flynt-list-posts"]
  *,
  *:before,
  *:after
    box-sizing: border-box

  .postList
    center($containerMaxWidth, $gutterWidth)

    &-posts
      margin: 30px 0

  @import 'partials/**/*.styl'
