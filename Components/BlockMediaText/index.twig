<div is="flynt-block-media-text" class="flyntComponent">
  <div class="blockMediaText {{ mediaPosition }}">
    <div class="blockMediaText-columnMedia">
      {% if mediaType == 'mediaImage' %}
        <img class="blockMediaText-imageElement" srcset="
          {{ image.src | resize(576, 324) }} 576w,
          {{ image.src | resize(768, 432) }} 768w"
          src="{{ image.src }}"
          sizes="(min-width: 1140px) 1140px, 100vw"
          alt="{{ image.alt }}">
      {% else %}
        <div class="blockMediaText-videoWrapper">
          <div class="blockMediaText-posterImage">
            <img class="blockMediaText-imageElement" srcset="
              {{ posterImage.src | resize(576, 324) }} 576w,
              {{ posterImage.src | resize(768, 432) }} 768w"
              src="{{ posterImage.src }}"
              sizes="(min-width: 1140px) 1140px, 100vw"
              alt="{{ posterImage.alt }}">
          </div>
          <div class="blockMediaText-video">
            {{ oembedLazyLoad }}
          </div>
        </div>
      {% endif %}
    </div>
    <div class="blockMediaText-columnContentHtml">
      {{ contentHTML }}
    </div>
  </div>
</div>
