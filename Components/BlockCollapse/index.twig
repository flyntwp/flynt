<flynt-component name="BlockCollapse" load:on="idle" {{ options.theme ? 'data-theme="' ~ options.theme ~ '"' }} data-status="{{ status }}" style="--percentage-distance: {{ percentageDistance }};"></flynt-component>
